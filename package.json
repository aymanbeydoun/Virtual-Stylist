{
  "name": "virtual-stylist",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "mobile": "pnpm --filter @virtual-stylist/mobile",
    "api": "pnpm --filter @virtual-stylist/api",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  }
}
